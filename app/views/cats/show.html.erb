<h1><%= @cat.name %></h1>

<pre><%= @cat.to_json %></pre>

<ul>
  <% @cat.toys.each do |toy| %>
  <li><%= toy.name %></li>
  <% end %>
</ul>

<p><a href="<%= new_cat_toy_url(@cat) %>">New Toy!</a></p>
<p><a href="<%= cats_url %>">Index</a></p>
