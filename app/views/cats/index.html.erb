<!DOCTYPE html>
<html lang="en">
  <head>
    <title>index</title>
    <meta charset="utf-8" />
  </head>
  <body>
    <h1>All the Cats</h1>
    <ul>
      <% @cats.each do |cat| %>
      <li>
        <a href="<%= cat_url(cat) %>"><%= cat.name %></a>
        <form action="<%= cat_url(cat) %>" method="post">
          <input type="hidden" name="_method" value="delete" />
          <button type="submit">Destroy Cat!</button>
        </form>
      </li>
      <% end %>
    </ul>
    <a href="<%= new_cat_url %>">New Cat</a>
  </body>
</html>
